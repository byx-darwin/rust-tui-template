# {{ project-name }} environment variables
# Copy this file to .env and fill in your values

# Log level (trace, debug, info, warn, error)
RUST_LOG=info

# App-specific settings
# APP_CONFIG_DIR=$HOME/.config/{{ project-name }}
